import styled from 'styled-components';

import {
	scale,
} from 'react-native-size-matters';


export const TouchableRadioButtonWrapper = styled.View`
	flex: 1
	flex-direction: row;
	margin-vertical: ${scale(9)};
`;